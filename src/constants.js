const CONSTANTS = {
  HEX_IN_ANGLE: (Math.PI * 2) / 6,
  HEX_START_ANGLE: (Math.PI / 6),
  TWO_PI: Math.PI * 2,
  RUN: 'RUN',
  PAINT: 'PAINT',
  RING: 'RING',
  STAMPS: {
    
  }
};

export default CONSTANTS;
